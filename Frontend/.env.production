# Built by `vite build` for production. Empty = same-origin relative API
# calls, since nginx serves the frontend and proxies /login, /profile, etc.
# to the backend on the same domain. Not needed locally (dev.sh runs
# frontend/backend on different ports, so constants.js falls back to the
# direct http://localhost:1234 URL instead).
VITE_API_BASE_URL=
